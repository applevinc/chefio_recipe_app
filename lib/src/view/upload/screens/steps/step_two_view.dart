import 'package:flutter/material.dart';

class StepTwoView extends StatelessWidget {
  const StepTwoView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('2nd page'),
    );
  }
}
